# Plan task <P0-T1>

Hướng dẫn thay thế: dùng file này làm không gian làm việc tạm thời cho task hiện tại. Khi task hoàn thành, rút gọn theo mục "Nội dung cần lưu lại sau khi reset".

## Vấn đề cần giải quyết

`<VẤN_ĐỀ_CẦN_GIẢI_QUYẾT>`

## Dữ liệu đã kiểm tra

- `<DỮ_LIỆU_ĐÃ_KIỂM_TRA_1>`.
- `<DỮ_LIỆU_ĐÃ_KIỂM_TRA_2>`.

## Giả định

- `<GIẢ_ĐỊNH_1>`.
- `<GIẢ_ĐỊNH_2>`.

## Câu hỏi đang mở

- `<CÂU_HỎI_1>`.
- `<CÂU_HỎI_2>`.

## Các phương án

### Phương án A - `<TÊN_PHƯƠNG_ÁN_A>`

`<MÔ_TẢ_PHƯƠNG_ÁN_A>`

### Phương án B - `<TÊN_PHƯƠNG_ÁN_B>`

`<MÔ_TẢ_PHƯƠNG_ÁN_B>`

## So sánh phương án

| Phương án | Ưu điểm | Nhược điểm | Ghi chú |
| --- | --- | --- | --- |
| A | `<ƯU_ĐIỂM_A>` | `<NHƯỢC_ĐIỂM_A>` | `<GHI_CHÚ_A>` |
| B | `<ƯU_ĐIỂM_B>` | `<NHƯỢC_ĐIỂM_B>` | `<GHI_CHÚ_B>` |

## Phương án được chốt

`<PHƯƠNG_ÁN_ĐƯỢC_CHỐT>`

## Lý do lựa chọn

`<LÝ_DO_LỰA_CHỌN>`

## Kế hoạch thực hiện

1. `<BƯỚC_1>`.
2. `<BƯỚC_2>`.
3. `<BƯỚC_3>`.

## Phát hiện trong quá trình làm

- `<PHÁT_HIỆN_1>`.
- `<PHÁT_HIỆN_2>`.

## Sai lệch so với kế hoạch

- `<SAI_LỆCH_1_HOẶC_KHÔNG_CÓ>`.

## Kết quả kiểm thử

- `<LỆNH_TEST_1>`: `<KẾT_QUẢ>`.
- `<LỆNH_TEST_2>`: `<KẾT_QUẢ>`.

## Nội dung cần lưu lại sau khi reset

- Task vừa hoàn thành: `<ID_TASK>`.
- Các phương án chính đã xem xét: `<TÓM_TẮT_PHƯƠNG_ÁN>`.
- Phương án được chọn: `<PHƯƠNG_ÁN_ĐƯỢC_CHỌN>`.
- Lý do: `<LÝ_DO>`.
- Phát hiện quan trọng: `<PHÁT_HIỆN_QUAN_TRỌNG>`.
- Kết quả kiểm thử: `<KẾT_QUẢ_KIỂM_THỬ>`.
- Vấn đề chuyển sang task sau: `<VẤN_ĐỀ_CHUYỂN_TIẾP>`.
